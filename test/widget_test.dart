// Basic smoke test — verifies the app can be built.
// Full integration tests are out of scope for v1.0.0.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
